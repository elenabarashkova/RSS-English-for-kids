export const getImagePath = (category: string, word: string) => (`./assets/category/${category}/${word}/${word}.jpg`);
export const getSoundPath = (category: string, word: string) => (`./assets/category/${category}/${word}/${word}.mp3`);
export const getCategoryImagePath = (name: string) => (`./assets/category/${name}/${name}.jpg`);